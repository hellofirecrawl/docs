```js Node
async function main() {
  // Crawl a website
  const crawlResponse = await app.crawlUrl(
    'https://firecrawl.dev',
    {
      limit: 100,
      scrapeOptions: {
        formats: ['markdown', 'html'],
      },
    }
  );

  console.log(crawlResponse);
}

main();
```
