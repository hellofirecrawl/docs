```python Python
# Crawl a website
crawl_response = app.crawl_url(
  url='https://firecrawl.dev',
  params={
    'limit': 100,
    'scrapeOptions': {
      'formats': ['markdown', 'html']
    }
  }
)

print(crawl_response)
```
