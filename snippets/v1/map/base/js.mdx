```js Node
import FirecrawlApp from '@mendable/firecrawl-js';

const app = new FirecrawlApp({
  apiKey: 'fc-YOUR_API_KEY'
});

async function main() {
  // Map a website
  const mapResponse = await app.mapUrl(
    'https://firecrawl.dev'
  );

  console.log(mapResponse);
}

main();
```
